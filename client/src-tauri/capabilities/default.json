{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "default",
  "description": "Capabilities for the main window: core defaults (needed for the first-run asset-setup event listeners) plus the shell plugin used to spawn the codesense-server/llama-server sidecars.",
  "windows": ["main"],
  "permissions": [
    "core:default",
    "core:event:default",
    "shell:allow-execute",
    "shell:allow-spawn"
  ]
}
